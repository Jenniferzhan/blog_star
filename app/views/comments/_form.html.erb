<%= form_for([@article, @article.comments.build]) do |f| %>
    <p>
    <%= f.label :姓名 %><br>
    <%= f.text_field :name%>
  </p>
  <p>
    <%= f.label :正文 %><br>
    <%= f.text_area :body %>
  </p>
  <p>
  <%= f.label :email %><br>
  <%= f.text_field :email %>
  </p>
  <p>
    <%= f.submit_tag"新增评论" %>
  </p>
<% end %>
